// @PROP[htProps_HatchDoor_WindowPlug]
// {
// 	MODULE
// 	{
// 		name = DisableCollider
// 		colliderName = doorHatchWindowPlug
// 	}
// }

// actually, since there's no way to disable the plug props, just remove them all
// it would be ideal to make a separate hatch prop that has the plug if necessary
@INTERNAL:HAS[@PROP[htProps_HatchDoor_WindowPlug]]:NEEDS[HabTechProps]
{
	-PROP[htProps_HatchDoor_WindowPlug],* {}
}
