@PROP[NF_HTCH_IVA_Cabin]:HAS[!MODULE[FreeIvaHatch]]:AFTER[NearFutureProps]
{
	MODEL
	{
		model = NearFutureProps/Props/Hatches/IVA/NF_HTCH_IVA_Cabin_Door
		position = -0.0447003953, 0, 0.223437443
	}

	MODULE
	{
		name = FreeIvaHatch
		doorTransformName = CabinDoor (1)

		Reparent
		{
			childTransformName = DoorWindow (1)
			parentTransformName = CabinDoor (1)
		}

		DoorCollider
		{
			shape = Box
			center = 0.03, 0, 0.22
			dimensions = 0.05, 0.9, 0.55
		}

		// TODO: the handle is on the same gameobject as the door, but ideally they need to be separate layers
	}
}

// remove all cabin doors from internals (it's just the door, and there should be a frame prop there)
@INTERNAL:HAS[@PROP[NF_HTCH_IVA_Cabin_Door]]
{
	-PROP[NF_HTCH_IVA_Cabin_Door],* {}
}