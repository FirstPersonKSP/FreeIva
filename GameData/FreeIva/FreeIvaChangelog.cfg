// Changelog file generated by yaclog-ksp (https://github.com/drewcassidy/yaclog-ksp)
KERBALCHANGELOG
{
    modName = FreeIva
    author = pizzaoverhead, JonnyOThan, Icecovery
    VERSION {
        version = 0.2.20.0
        versionDate = 2024-11-11
        CHANGE {
            type = Notable Changes
            change = Added support for [Simplex station parts habs](https://spacedock.info/mod/3644/SIMPLEX%20Station%20Parts%20Habs)
        }

        CHANGE {
            type = Notable Changes
            change = Add passthrough options for stock 1.25m fuel tanks
        }

        CHANGE {
            type = Notable Changes
            change = Reduced logging verbosity to improve loading performance
        }

        CHANGE {
            type = Notable Changes
            change = Improve performance overhead of FreeIva part modules
        }

        CHANGE {
            type = Notable Changes
            change = Adjusted depth mask/window rendering order to fix lots of clipping issues at the borders of windows
        }

        CHANGE {
            type = Notable Changes
            change = Close hatches when undocking/decoupling when necessary
        }

        CHANGE {
            type = Notable Changes
            change = Hatch tubes are now hidden when IVA overlay mode is enabled
        }

        CHANGE {
            type = Notable Changes
            change = Added sounds to food/drink props (and one bonus prop)
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a bug causing hatches in the Pathfinder Hacienda module to not be connected properly
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a bug that could cause you to fall out of the world when boarding the Planetside MMSEV rover from EVA
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a bug where held props could disappear if you buckled and unbuckled
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a bug that would break the flight camera if the active kerbal died while in a separate part
        }

    }

    VERSION {
        version = 0.2.19.0
        versionDate = 2024-09-23
        CHANGE {
            type = Notable Changes
            change = Added es-es localization (thanks 9field and masertank!)
        }

        CHANGE {
            type = Notable Changes
            change = Added support for [Stockalike Mining Extension](https://forum.kerbalspaceprogram.com/topic/130325-110x-stockalike-mining-extension-115-release-782020/)
        }

        CHANGE {
            type = Notable Changes
            change = Added support for [Kerbal Changelog](https://forum.kerbalspaceprogram.com/topic/200702-19%E2%80%93112-kerbal-changelog-v142-adopted/)
        }

        CHANGE {
            type = Notable Changes
            change = Added passthrough for mk2 cargo bays
        }

        CHANGE {
            type = Notable Changes
            change = Now compatible with Deferred rendering
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed centrifuges not working properly with Kerbalism
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed potential NRE spam with certain combinations of mods
        }

        CHANGE {
            type = Bug Fixes
            change = NeistAir: Fixed broken passthrough configs
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a B9PartSwitch error when using Mk2Rebalance
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a B9PartSwitch error when using GalacticTrading
        }

    }

    VERSION {
        version = 0.2.18.4
        versionDate = 2024-03-03
        CHANGE {
            type = Notable Changes
            change = Habtech CBM hatches now have a blocked variant
        }

        CHANGE {
            type = Notable Changes
            change = Stock mk3 crew cabin now has ventral and dorsal hatches and attachnodes
        }

        CHANGE {
            type = Notable Changes
            change = Open animation limit can now be configured per hatch
        }

        CHANGE {
            type = Notable Changes
            change = B9PSConditionalProp can now specify multiple subtypes
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some missing texture errors
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug where using the seats in the inline docking ports could cause the IVA kerbal to stop spawning
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug preventing requireDeploy in the internal module from working
        }

        CHANGE {
            type = Bug Fixes
            change = Improved error reporting when a cutout target isn't found
        }

        CHANGE {
            type = Bug Fixes
            change = Prevent going EVA from hatches connected to deactivated airlocks
        }

        CHANGE {
            type = Bug Fixes
            change = "Close forever" button on the tutorial window will now keep it closed until the next KSP restart instead of per scene
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed auto-detection system for internal spaces that are only referenced via Reviva configs (fixes hatches in some warbirds cockpits when used with Reviva, and probably others)
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed centrifugal force calculation for spinning parts so that it points towards the axis of rotation rather than center of mass
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed broken camera when boarding from EVA before IVA had ever been used since the last scene switch
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed boarding behavior not working when using a kerbal that had not gone EVA since the last scene switch
        }

        CHANGE {
            type = Bug Fixes
            change = Planetside: Fix hatches not changing properly per subtype in the vertical base node
        }

        CHANGE {
            type = Bug Fixes
            change = Planetside: Fix going EVA from some parts when alternate endcaps were used
        }

        CHANGE {
            type = Bug Fixes
            change = Restock: Fix error message when using mk1 lander
        }

        CHANGE {
            type = Bug Fixes
            change = Restock: Fix inflatable airlock
        }

        CHANGE {
            type = Bug Fixes
            change = Restock: Fixed hatches in mk2landercan
        }

        CHANGE {
            type = Bug Fixes
            change = SSPX: inflatable habs and centrifuges no longer show the internal in the IVA overlay when not inflated/expanded
        }

        CHANGE {
            type = Bug Fixes
            change = SXT: Fixed hatches and cutouts in large passenger cabin
        }

        CHANGE {
            type = Bug Fixes
            change = SXT: Fixed a bad model reference in the 6-way hub
        }

        CHANGE {
            type = Bug Fixes
            change = KNES: Fixed dorsal hatch on French Hermes crew module and docking module
        }

        CHANGE {
            type = Bug Fixes
            change = Tantares: Included v2 parts in support patches
        }

        CHANGE {
            type = Bug Fixes
            change = ACK: Fixed blocked bottom hatch in HLS
        }

        CHANGE {
            type = Bug Fixes
            change = ACK: Fixed wrong cutout target in HLS
        }

    }

    VERSION {
        version = 0.2.18.3
        versionDate = 2023-11-14
        CHANGE {
            type = Bug Fixes
            change = Fixed a new bug related to SSPX centrifuges
        }

        CHANGE {
            type = Bug Fixes
            change = Fix camera glitch when boarding a part from EVA when QuickIVA is installed
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a bug that would break IVA mode when switching vessels while unbuckled
        }

        CHANGE {
            type = Bug Fixes
            change = Fix exception spew when the internal camera was destroyed
        }

    }

    VERSION {
        version = 0.2.18.2
        versionDate = 2023-11-09
        CHANGE {
            type = Bug Fixes
            change = Removed ALCOR and ASET hatch configs (these are now in their respective mods)
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some issues with Artemis Construction Kit
        }

    }

    VERSION {
        version = 0.2.18.1
        versionDate = 2023-11-06
        CHANGE {
            type = Notable Changes
            change = Turkish localization (thanks @Ferristik!)
        }

    }

    VERSION {
        version = 0.2.18.0
        versionDate = 2023-11-05
        CHANGE {
            type = Notable Changes
            change = Added support for Lonesome Robots Collection
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Tiktaalik Dreaming Props
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Pathfinder
        }

        CHANGE {
            type = Notable Changes
            change = French localization (thanks @tony48 !)
        }

        CHANGE {
            type = Notable Changes
            change = Added support for RealChute's stack chute
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Cormorant Aeronology
        }

        CHANGE {
            type = Notable Changes
            change = Centrifugal acceleration from rotating parts is now simulated
        }

        CHANGE {
            type = Notable Changes
            change = Boarding a part from EVA now starts you unbuckled just inside the hatch. You can press C to immediately return to your seat, or disable this behavior in the settings.cfg file.
        }

        CHANGE {
            type = Bug Fixes
            change = Fix JSI EVA hatches in certain cockpits
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed several issues in OPT
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed several bugs that could result in exception spamming, loss of control, or inability to unbuckle
        }

        CHANGE {
            type = Bug Fixes
            change = Better support for WildBlue props
        }

        CHANGE {
            type = Bug Fixes
            change = Stock 0.625m decoupler and separator are now passable to be consistent with Connected Living Space
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed several issues in Tantares
        }

        CHANGE {
            type = Bug Fixes
            change = Hatches that are set to auto-hide when connected no longer hide their paired hatch if it has an animation
        }

        CHANGE {
            type = Bug Fixes
            change = Hatches in jr docking port and benjee�s docking ports are now set to hide when connected
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed texture on the back of Near Future Props EVA hatch
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed cutout and depth mask in SSPX 3.75m greenhouse
        }

        CHANGE {
            type = Bug Fixes
            change = Added ladder support to Habtech Props bunkbed
        }

    }

    VERSION {
        version = 0.2.17.1
        versionDate = 2023-09-20
        CHANGE {
            type = Bug Fixes
            change = Fix fuel tank masses when NFLV is installed without CyroTanks or Near Future Construction
        }

    }

    VERSION {
        version = 0.2.17.0
        versionDate = 2023-09-19
        CHANGE {
            type = Notable Changes
            change = Added support for Heisenberg Airships
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Wildblue physical props
        }

        CHANGE {
            type = Notable Changes
            change = Added support for DeepFreeze
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Near Future Launch Vehicles
        }

        CHANGE {
            type = Bug Fixes
            change = Fix some issues with Mk2Expansion
        }

        CHANGE {
            type = Bug Fixes
            change = Added missing parameters to settings.cfg (jump key, jumpforce, walkable slope)
        }

        CHANGE {
            type = Bug Fixes
            change = Support parts that don't use mesh colliders
        }

        CHANGE {
            type = Bug Fixes
            change = Fix NRE when airlock is missing
        }

        CHANGE {
            type = Bug Fixes
            change = Tourists can no longer go EVA
        }

        CHANGE {
            type = Bug Fixes
            change = Now checks for obstructions before playing the animation on EVA hatches
        }

        CHANGE {
            type = Bug Fixes
            change = Parts without inherent tags now get the freeiva tag and category correctly
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed KPBS landing engine passthrough
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some issues with Exploration Rover Systems
        }

        CHANGE {
            type = Bug Fixes
            change = Smoothed out roll inputs in zero-g
        }

    }

    VERSION {
        version = 0.2.16.1
        versionDate = 2023-07-02
        CHANGE {
            type = Bug Fixes
            change = Fixed hatch positions and narrow colliders in KPBS central hub
        }

    }

    VERSION {
        version = 0.2.16.0
        versionDate = 2023-06-29
        CHANGE {
            type = Notable Changes
            change = Added support for Exploration Rover System
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Keridian Dynamics
        }

        CHANGE {
            type = Notable Changes
            change = Added support for The Martian
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Malemute Rover
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Coriolis Space Systems
        }

        CHANGE {
            type = Notable Changes
            change = Added physical props for Knes, Tantares, and BDB
        }

        CHANGE {
            type = Notable Changes
            change = Squad fire extinguisher is now a prop (placed in mk1 and mk2 lander cans)
        }

        CHANGE {
            type = Notable Changes
            change = Cargo bags in mk2Inline are now interactable
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch interactions for hatches without handles
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch interactions for non-prop hatches
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed Mk2Expansion shielded docking port
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed targeting seats when they have a non-identity scale
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed going EVA from stock docking ports
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some issues with stationhub hatch placement
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed tube scaling when hatches were not aligned
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed Knes ECBM hatches
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed OPT J cockpits
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed restock plus inflatable airlock
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch in habtech2 airlock module
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed BDB Dona pod hatch
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed conflicts with RealismOverhaul
        }

    }

    VERSION {
        version = 0.2.15.2
        versionDate = 2023-05-15
        CHANGE {
            type = Bug Fixes
            change = Fixed MM syntax error in b9aerospace passthrough patches
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed shell collider in Planetside MMSEV
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed stationhub RPM configuration so that wall labels can work again
        }

        CHANGE {
            type = Bug Fixes
            change = Added physical prop support for the MAS version of ASET beverage props
        }

        CHANGE {
            type = Bug Fixes
            change = Converted cargo bags in mk2 cockpit to physical props
        }

        CHANGE {
            type = Bug Fixes
            change = Prevent grabbing props from flight camera (IVA Overlay)
        }

        CHANGE {
            type = Bug Fixes
            change = Clicking hatch buckle buttons from flight camera (IVA Overlay) now works as expected
        }

        CHANGE {
            type = Bug Fixes
            change = Hatches with hideDoorWhenOpen (most docking ports) now also hide their connected hatch like they used to, so that hatches without animations don't get in the way
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed cutout mesh on NF_HTCH_EVA_BASIC
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed chair configuration in mk3expansion's mk3inline cockpit
        }

        CHANGE {
            type = Bug Fixes
            change = Split BDB content into a separate download
        }

    }

    VERSION {
        version = 0.2.15.1
        versionDate = 2023-05-03
        CHANGE {
            type = Notable Changes
            change = Animated hatch support. All stock and near future props hatches are now animated. Remember you can click the handle to open it!
        }

        CHANGE {
            type = Bug Fixes
            change = Fix some passthrough issues in Near Future Construction and KNES
        }

        CHANGE {
            type = Bug Fixes
            change = Coffee mug in ProbeControlRoom is now interactable
        }

        CHANGE {
            type = Bug Fixes
            change = Use landed gravity threshold in centrifuges
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug that could prevent opening hatches in centrifuges
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed window depth masks on stock parts
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed NRE spew when going EVA when ThroughTheEyes isn't installed
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed softlock when going EVA from inline docking ports
        }

    }

    VERSION {
        version = 0.2.14.0
        versionDate = 2023-04-24
        CHANGE {
            type = Notable Changes
            change = Added Near Future Construction support
        }

        CHANGE {
            type = Notable Changes
            change = Added Cold War Aerospace support
        }

        CHANGE {
            type = Notable Changes
            change = Added Tundra Exploration support
        }

        CHANGE {
            type = Bug Fixes
            change = Fix hatch cutouts on scaled hatches (introduced in 0.2.13.0)
        }

        CHANGE {
            type = Bug Fixes
            change = Fix missing FreeIva modules on FelineUtilityRover, and the collider on the docking bay
        }

        CHANGE {
            type = Bug Fixes
            change = Fix Kerbal Planetary Base System hatches when RPM is installed
        }

    }

    VERSION {
        version = 0.2.13.0
        versionDate = 2023-04-19
        CHANGE {
            type = Notable Changes
            change = New physical prop system: alt-left-click to grab or throw a prop. Left-click to use the prop (if appropriate). The ALCOR pod is a great testbed for this. Near Future Spacecraft, Stockalike Station Parts Expanded, Habtech2, and Planetside are also good spaces to explore.
        }

        CHANGE {
            type = Notable Changes
            change = Added OPT support
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a fatal bug when a camera collider transform is not found (B9 + RPM)
        }

        CHANGE {
            type = Bug Fixes
            change = Added passthrough variant to 1.8m and 2.5m monoprop tanks (requires B9PartSwitch)
        }

    }

    VERSION {
        version = 0.2.12.0
        versionDate = 2023-04-08
        CHANGE {
            type = Notable Changes
            change = Added support for NeistAir
        }

        CHANGE {
            type = Notable Changes
            change = Added support for AirplanePlus
        }

        CHANGE {
            type = Notable Changes
            change = Added support for SXT
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Kerbonov
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Warbirds Cockpits
        }

        CHANGE {
            type = Bug Fixes
            change = Disable autopassthrough system when SimpleFuelSwitch is installed
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed several textures in BDB internals
        }

    }

    VERSION {
        version = 0.2.11.1
        versionDate = 2023-04-01
        CHANGE {
            type = Bug Fixes
            change = Fixed SOCK middeck ceiling collider
        }

    }

    VERSION {
        version = 0.2.11.0
        versionDate = 2023-03-29
        CHANGE {
            type = Notable Changes
            change = Added support for KNES
        }

        CHANGE {
            type = Notable Changes
            change = Added preliminary support for Bluedog Design Bureau. Note this must be installed manually on top of your BDB install, replacing any files as necessary. Eventually this will be included in BDB itself.
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Mk3 Mini Expansion
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Starilex's mk1pod Needle IVA
        }

        CHANGE {
            type = Notable Changes
            change = Added support for SABS IVAs
        }

        CHANGE {
            type = Notable Changes
            change = Added support for ReStockPlus
        }

        CHANGE {
            type = Notable Changes
            change = Added localization support (thanks to @tinygrox !) And thanks to all the translators! If you'd like to translate FreeIva into your language, please get in touch on discord.
        }

        CHANGE {
            type = Notable Changes
            change = Added passthrough support to Near Future Spacecraft's orbital engine
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed m2x Angler seat config
        }

        CHANGE {
            type = Bug Fixes
            change = Added a bunch of missing textures
        }

        CHANGE {
            type = Bug Fixes
            change = Adjusted the restriction for unbuckling to support probe cores/ProbeControlRoom
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some visual issues with habtech docking ports
        }

        CHANGE {
            type = Bug Fixes
            change = Changed top hatch configuration for Artemis Construction Kit Orion pod: the docking port should now be attached to the parachute cover (should now work with the included craft file)
        }

        CHANGE {
            type = Bug Fixes
            change = Added EVA support to structural tubes. The 1.25m structural fuselage no longer has an IVA but you can EVA through it.
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some bugs where other internals would disappear on vessel changed events or switching kerbals
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed planetside adapter part preventing opening hatches on the other side
        }

        CHANGE {
            type = Bug Fixes
            change = Added support for old mk2landerCan internals
        }

        CHANGE {
            type = Bug Fixes
            change = Prevent double-clicking on camera colliders when unbuckled
        }

        CHANGE {
            type = Bug Fixes
            change = Increased the acceleration threshold for orienting to the horizon when not landed
        }

        CHANGE {
            type = New Known Issues
            change = Centrifuge internals are always visible when JSI Advanced Transparent Pods is installed
        }

    }

    VERSION {
        version = 0.2.10.3
        versionDate = 2023-03-10
        CHANGE {
            type = Notable Changes
            change = The KV2Pod and KV3Pod from Making History now have their external hatch moved to match where the internal hatch is located. This may break certain craft designs if that location is occupied. You can delete GameData/FreeIva/SquadExpansion/PartCfgs/KVPod_Airlocks.cfg to restore the default airlock location.
        }

        CHANGE {
            type = Notable Changes
            change = Unbuckling in non-supported parts is no longer allowed (underlying code changes made this more difficult). As always, if you'd like to see FreeIva support for your favorite IVA mods, let me know on the forum or discord.
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed dockingPortSr showing up in external view
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed more issues with kerbal portraits in conjunction with ProbeControlRoom
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed broken centrifuge movement
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bunch of issues with Tantares
        }

    }

    VERSION {
        version = 0.2.10.2
        versionDate = 2023-03-05
        CHANGE {
            type = Notable Changes
            change = All 3rd-party mod patches are now marked as `:FOR[FreeIva]` so that if another mod wishes to assume ownership of their FreeIva configs, they should use `:BEFORE[FreeIva]`
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a function signature change that broke KerbalVR in 0.2.10.1
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed flags becoming transparent in ProbeControlRoom
        }

    }

    VERSION {
        version = 0.2.10.1
        versionDate = 2023-03-05
        CHANGE {
            type = Bug Fixes
            change = KOSPropMonitor now locks out keys properly
        }

        CHANGE {
            type = Bug Fixes
            change = Pressing M while unbuckled no longer breaks the cameras
        }

    }

    VERSION {
        version = 0.2.10.0
        versionDate = 2023-03-02
        CHANGE {
            type = Notable Changes
            change = Added support for Feline Utility Rover
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Tantares
        }

        CHANGE {
            type = Notable Changes
            change = Added a ladder collider to NearFutureProps cargo nets
        }

        CHANGE {
            type = Notable Changes
            change = Added ASET wall labels to the stock station hub. Now you can label your hatches!
        }

        CHANGE {
            type = Notable Changes
            change = HabTechProps and NearFutureProps hatches are now compatible with KerbalVR
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed shadow casters in HabTech2 parts
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed depth mask on Planetside MMSEV
        }

        CHANGE {
            type = Bug Fixes
            change = Fix border mesh on FLA10 adapter part rendering in the wrong location
        }

    }

    VERSION {
        version = 0.2.9.2
        versionDate = 2023-02-18
        CHANGE {
            type = New Dependencies
            change = CommunityCategoryKit is recommended to see all FreeIVA-supported parts at once. You can also type "freeiva" into the stock search box, but may get some false positives.
        }

        CHANGE {
            type = Notable Changes
            change = Added "freeiva" tag to all traversable parts so they can be found via stock search box
        }

        CHANGE {
            type = Notable Changes
            change = Added support for CommunityCategoryKit, which will display all FreeIva-supported parts
        }

        CHANGE {
            type = Notable Changes
            change = Added support for late additions to HabTech2/benjee10's sharedAssets: The ORB and hybrid docking ports
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug where the gravity direction in ProbeControlRoom would be incorrect if the probe core was rotated relative to the root part
        }

    }

    VERSION {
        version = 0.2.9.1
        versionDate = 2023-02-13
        CHANGE {
            type = Notable Changes
            change = Added support for the upcoming HabTech2 IVA release
        }

        CHANGE {
            type = Notable Changes
            change = Allow free movement and orientation in 0.05 m/s/s or less (e.g. Gilly's surface), but still apply subjective gravity
        }

        CHANGE {
            type = Notable Changes
            change = Add sounds for buckling/unbuckling
        }

        CHANGE {
            type = Notable Changes
            change = Added a system to auto-detect hatch configurations
        }

    }

    VERSION {
        version = 0.2.8.0
        versionDate = 2023-02-10
        CHANGE {
            type = Notable Changes
            change = Added support for ProbeControlRoom
        }

        CHANGE {
            type = Notable Changes
            change = You can now switch to a kerbal by pressing V when aiming at them
        }

        CHANGE {
            type = Notable Changes
            change = Acceleration forces from flight are fully modeled
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed ExtraDockingPorts support
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bunch of (harmless) errors and warnings
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed shadows in stock Mobile Processing Lab
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed ALCOR top hatch
        }

        CHANGE {
            type = Bug Fixes
            change = Prepping for better KerbalVR support
        }

    }

    VERSION {
        version = 0.2.7.0
        versionDate = 2023-02-01
        CHANGE {
            type = Notable Changes
            change = Added support for Universal Storage II
        }

        CHANGE {
            type = Notable Changes
            change = Added support for the ALCOR pod (yes it still works in 1.12!)
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed mk2Expansion and mk3Expansion passthrough when CryoTanks is installed
        }

        CHANGE {
            type = Bug Fixes
            change = Better integration with RasterPropMonitor (requires 0.31.10.2 or later) so that you can interact with props after moving to another part
        }

    }

    VERSION {
        version = 0.2.6.1
        versionDate = 2023-01-30
        CHANGE {
            type = Notable Changes
            change = Disallow unbuclking in mk2Expansion Tuna/fishhead cockpit
        }

        CHANGE {
            type = Notable Changes
            change = Add lights to SSPX centrifuges
        }

    }

    VERSION {
        version = 0.2.6.0
        versionDate = 2023-01-28
        CHANGE {
            type = Notable Changes
            change = Added support for Artemis Construction Kit
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Shuttle Orbiter Construction Kit. Includes brand-new (very bad) IVAs for the spacelab parts.
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Mark IV Spaceplane System. Reviva is recommended for support in adapter parts.
        }

        CHANGE {
            type = Notable Changes
            change = Added support for B9 Aerospace
        }

        CHANGE {
            type = Notable Changes
            change = Passthrough system is more intelligent: disabling is done on a per-part basis and the part's info box will have details about volume penalties, structural variants, or incompatibilities.
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed shadowing inside hatch tubes
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some config issues in Mk2Expansion
        }

        CHANGE {
            type = Bug Fixes
            change = Added a light to the large rockomax adapter
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed orientation issues with stack bicoupler and large quadcoupler
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed depth mask and border on inline docking ports
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed tube lengths on docking ports and when parts are offset
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug in shell collider bounds that could break interactions after passing through a docking port
        }

    }

    VERSION {
        version = 0.2.5.2
        versionDate = 2023-01-24
        CHANGE {
            type = Notable Changes
            change = Added support for the ASET mk1-2 IVA (Use Reviva to use this IVA in the mk1-3 pod).
        }

        CHANGE {
            type = Notable Changes
            change = Revamped models in all stock multi-adapter parts (Thanks @Icecovery )
        }

        CHANGE {
            type = Notable Changes
            change = Added passthrough support to 1.875m tanks from Making History
        }

        CHANGE {
            type = Bug Fixes
            change = Disabled fuel tank passthrough configs when ModularFuelTanks or ConfigurableContainers is installed
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch issue in mk1-4 pod from NearFutureSpacecraft
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch and collisions issues in some SSPX parts
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed an issue with "temporary" seats like the one in the inline docking port
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed RPM configuration in inline docking port
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch tubes not using the proper size when the attachnodes were changed by b9ps (e.g. planetside endcaps)
        }

        CHANGE {
            type = Bug Fixes
            change = Changed shadow and lighting configuration in the internal space
        }

    }

    VERSION {
        version = 0.2.5.1
        versionDate = 2023-01-19
        CHANGE {
            type = Bug Fixes
            change = Improved performance when unbuckled or in VR
        }

    }

    VERSION {
        version = 0.2.5.0
        versionDate = 2023-01-17
        CHANGE {
            type = New Dependencies
            change = B9PartSwitch is recommended for passable variants of some of the stock parts.
        }

        CHANGE {
            type = New Dependencies
            change = RasterPropMonitor and ASET props are recommended for the stock inline docking ports.
        }

        CHANGE {
            type = Notable Changes
            change = Added Mk2 Expansion support, including all of its structural pieces. Reviva is recommended.
        }

        CHANGE {
            type = Notable Changes
            change = Added Mk3 Expansion support, including all of its structural pieces. Reviva is recommended.
        }

        CHANGE {
            type = Notable Changes
            change = Added Near Future Spacecraft support, including the Vexarp IVA layouts.
        }

        CHANGE {
            type = Notable Changes
            change = If B9PartSwitch is installed all stock adapter, Rockomax, and mk3 tanks get an option to add a passable crew tube with a penalty to resource capacity and mass ratio. Slanted adapters also require Reviva.
        }

        CHANGE {
            type = Notable Changes
            change = Revamped internal models for docking ports, large Rockomax adapter, and FLA10 adapter.
        }

        CHANGE {
            type = Notable Changes
            change = Inline docking ports now have a dedicated control console with RPM support for docking
        }

        CHANGE {
            type = Notable Changes
            change = Added an options to disable collisions and the tutorial window (see settings.cfg)
        }

        CHANGE {
            type = Bug Fixes
            change = Habtech door prop gets a window plug when it's blocked
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some issues in planetside crew tubes
        }

        CHANGE {
            type = Bug Fixes
            change = NFProps Cabin door is now clickable
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug where probe control room internals could be incorrectly created
        }

        CHANGE {
            type = Bug Fixes
            change = Added ability for hatches to use depth masks to make sure you can't see distant internal models through their windows
        }

        CHANGE {
            type = Bug Fixes
            change = Clicking on a hatch handle doesn't bypass connection checks
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a bug where hatches would not be connected after docking
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed an issue that could block mouse clicks
        }

    }

    VERSION {
        version = 0.2.4
        versionDate = 2023-01-03
        CHANGE {
            type = Notable Changes
            change = Support for Kerbalism's GravityRing and Greenhouse parts
        }

        CHANGE {
            type = Notable Changes
            change = SSPX centrifuges no longer break when Kerbalism is installed
        }

        CHANGE {
            type = Notable Changes
            change = Support for modded mk3 cockpits: KermanTech, Ultimate Shuttle IVA, and Mk3 Shuttle Mid-Deck
        }

        CHANGE {
            type = Notable Changes
            change = Support for Extra Docking Ports
        }

        CHANGE {
            type = Notable Changes
            change = Support for SDHI Service Module System
        }

        CHANGE {
            type = Notable Changes
            change = All stock part EVA hatches now have an attachment node so you can connect other crewed parts there to pass through
        }

        CHANGE {
            type = Notable Changes
            change = Added tutorial window and additional button prompts
        }

        CHANGE {
            type = Bug Fixes
            change = Raised camera height when standing in gravity
        }

        CHANGE {
            type = Bug Fixes
            change = Internals of distant parts are no longer visible when looking out windows
        }

        CHANGE {
            type = Bug Fixes
            change = Can no longer interact with hatches and seats through walls
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed an issue that would occasionally block mouse clicks while unbuckled
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed some issues related to crew transfers
        }

        CHANGE {
            type = Bug Fixes
            change = Tweaked console collider in mk1pod so you can squeeze under it
        }

    }

    VERSION {
        version = 0.2.3
        versionDate = 2022-12-17
        CHANGE {
            type = New Dependencies
            change = HabTech Props is required for Kerbal Planetary Base Systems
        }

        CHANGE {
            type = Notable Changes
            change = Support for Kerbal Planetary Base Systems - Requires HabTech Props
        }

        CHANGE {
            type = Bug Fixes
            change = Fix some issues caused by restock changes
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed hatch tubes appearing in unsupported parts
        }

        CHANGE {
            type = Bug Fixes
            change = Other minor stuff
        }

    }

    VERSION {
        version = 0.2.2
        versionDate = 2022-12-15
        CHANGE {
            type = Notable Changes
            change = Movement in gravity (including crouching, jumping and ladders) is fully supported. See the Controls page for more info
        }

        CHANGE {
            type = Notable Changes
            change = Full support for Stockalike Station Parts Expansion Redux
        }

        CHANGE {
            type = Notable Changes
            change = Full support for Buffalo 2
        }

        CHANGE {
            type = Notable Changes
            change = The centrifuges from SSPX will provide artificial gravity. See the Controls page for more info.
        }

        CHANGE {
            type = Notable Changes
            change = Faster and more accurate mesh cutter
        }

        CHANGE {
            type = Notable Changes
            change = Added support for Planetside adapter parts
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed leaking OnCameraChanged event handler
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed jittering in movement
        }

        CHANGE {
            type = Bug Fixes
            change = Fix NREs when leaving IVA mode
        }

        CHANGE {
            type = Bug Fixes
            change = The bottoms of stock docking ports are now solid when not attached to something traversible
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed a rendering order issue with transparent geometry
        }

        CHANGE {
            type = Bug Fixes
            change = Scaled hatch props now scale their tubes properly
        }

        CHANGE {
            type = Bug Fixes
            change = Added an internal model to the mk1 structural fuselage
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed cutout meshes (hexagonal prisms, etc) being left behind when the blocked prop was used
        }

        CHANGE {
            type = New Known Issues
            change = When using a part that swaps internals with Reviva, physics will go crazy on the first launch of the vessel. Reverting to launch should fix it. NOTE this has been fixed in Reviva 0.8
        }

        CHANGE {
            type = New Known Issues
            change = The internals of distant parts are visible through windows
        }

    }

    VERSION {
        version = 0.2.1
        versionDate = 2022-11-24
        CHANGE {
            type = New Dependencies
            change = Reviva is recommended if you use the vertical node part from Planetside - it has several different configurations, and Reviva is necessary to alter the internal models to match the external configuration.
        }

        CHANGE {
            type = Notable Changes
            change = All parts from Planetside Exploration Technologies are now supported
        }

        CHANGE {
            type = Notable Changes
            change = Can now board a part from EVA even if there are no empty seats in it
        }

        CHANGE {
            type = Notable Changes
            change = Can now go EVA from stock docking ports
        }

        CHANGE {
            type = Notable Changes
            change = Movement now uses momentum and acceleration
        }

        CHANGE {
            type = Notable Changes
            change = Hatches that are blocked on the other side will change their appearance
        }

        CHANGE {
            type = Notable Changes
            change = Adjust inline docking port internals
        }

        CHANGE {
            type = Bug Fixes
            change = Fix hatch open sound playing on spawn
        }

        CHANGE {
            type = Bug Fixes
            change = Fixed kerbal heads re-appearing
        }

        CHANGE {
            type = Bug Fixes
            change = Fix jittery camera at pitch limits
        }

        CHANGE {
            type = Bug Fixes
            change = Ships with loops of parts (self-docking) now work
        }

        CHANGE {
            type = Bug Fixes
            change = Adjusted colliders in mk1 and mk1-3 pods
        }

        CHANGE {
            type = Bug Fixes
            change = Don't spawn ProbeControlRoom IVAs for probes
        }

        CHANGE {
            type = Bug Fixes
            change = Fix a model problem in dockingPortJr
        }

        CHANGE {
            type = Bug Fixes
            change = remove ModuleFreeIva from cloned RP-0 parts
        }

        CHANGE {
            type = Bug Fixes
            change = Fix some non-serious errors
        }

        CHANGE {
            type = New Known Issues
            change = Windows in distant parts do not render correctly
        }

    }

    VERSION {
        version = 0.2
        versionDate = 2022-11-09
        change = All stock and DLC crewed parts and docking ports (and several adapters) are supported
        change = Fixed many buckling/unbuckling issues
        change = You can now go EVA from most hatches that aren't connected to anything
        change = Added custom hatch props with clickable handles
        change = Added custom harness and seatbelt props with clickable buttons
        change = IVA colliders can now be built in regular MODEL nodes
        change = Sorted out collision layer issues
        change = Lots of other stuff
    }

    VERSION {
        version = 0.1.2
        versionDate = 2021-03-27
        change = Added placeholder Station Hub IVA.
        change = Added converter for using part colliders as IVa colliders.
        change = Added floor colliders for all IVA parts.
        change = Fixed error on part destroyed.
    }

    VERSION {
        version = 0.1.1
        versionDate = 2021-03-21
        change = Rebuilt for KSP 1.11.
        change = Moved to github.
        change = Improved gravity mode.
        change = Added jumping.
        change = Fixed issues with craft containing multiple instances of the same part.
        change = GUI cleanup.
        change = Made sounds configurable.
        change = Assorted small bugfixes.
    }

}
